import React from 'react';

const TShirt = ({tShirt}) => {

    const {name, picture, price} = tShirt;

    console.log(tShirt);

    return (
        <div>
            
        </div>
    );
};

export default TShirt;